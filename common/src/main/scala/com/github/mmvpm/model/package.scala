package com.github.mmvpm

import java.util.UUID

package object model {
  type OfferID = UUID
  type UserID = UUID
  type Email = String
  type Phone = String
  type Money = Int
  type Session = UUID
}
